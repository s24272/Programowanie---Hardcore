#include <iostream>

void init(int tab[],int n)
{
    for(int i=0;i<n;i++)
    {
        tab[i]=0;
    }
}
int main()
{
    int n;
    int tab[1000];
    std::cout<<"Podaj rozmiar tablicy: \n";
    std::cin>>n;
    init(tab, n);
    return 0;
}
